{
  "openapi": "3.0.0",
  "info": {
    "title": "Microsoft 365 Roadmap API",
    "description": "API for accessing Microsoft 365 roadmap items with OData query capabilities. Use 'created' field for date filtering (not 'createdDateTime').",
    "version": "0.0.0"
  },
  "tags": [],
  "paths": {
    "/m365": {
      "get": {
        "operationId": "RoadmapOperations_getM365RoadmapInfo",
        "description": "Get Microsoft 365 roadmap items\n\nRetrieve roadmap items with optional filtering, sorting, and pagination.\nAlways use 'created' field for date operations, not 'createdDateTime'.",
        "parameters": [
          {
            "$ref": "#/components/parameters/Parameters.FilterParameter"
          },
          {
            "$ref": "#/components/parameters/Parameters.OrderByParameter"
          },
          {
            "$ref": "#/components/parameters/Parameters.TopParameter"
          },
          {
            "$ref": "#/components/parameters/Parameters.SkipParameter"
          },
          {
            "$ref": "#/components/parameters/Parameters.CountParameter"
          }
        ],
        "responses": {
          "200": {
            "description": "The request has succeeded.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Models.RoadmapResponse"
                }
              }
            }
          },
          "400": {
            "description": "The server could not understand the request due to invalid syntax.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Models.ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Models.ErrorResponse"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "parameters": {
      "Parameters.CountParameter": {
        "name": "$count",
        "in": "query",
        "required": true,
        "description": "Include total count of items in response",
        "schema": {
          "type": "boolean",
          "default": true
        },
        "explode": false
      },
      "Parameters.FilterParameter": {
        "name": "$filter",
        "in": "query",
        "required": false,
        "description": "OData filter expression. IMPORTANT: Use 'created' field for created date filtering (not 'createdDateTime'). Use the 'modified' field for last modified date filtering (not 'lastModifiedDateTime'). Supports contains(), tolower(), and date comparisons. Filter the results based on specific conditions. Use `contains(tolower(title),tolower('search term'))` to search within the title property for a specific term. For creation date filtering, use the 'created' property with ISO 8601 date format. For example, `created ge 2025-03-01T00:00:00Z` to filter roadmap items created after March 1, 2025. To combine filters, use `and` operator. For example, `contains(tolower(title),tolower('Teams')) and created ge 2025-03-01T00:00:00Z`. For the 'status' field, you can filter by status using `status eq 'In development'`, `status eq 'Rolling out'`, `status eq 'Launched'`, `status eq 'Preview'`, `status eq 'Planned'`, or `status eq 'Cancelled'`. For array fields, use the any() operator: For 'platforms' array, use `platforms/any(p: contains(tolower(p), 'desktop'))` to find items available on desktop platform, or `platforms/any(p: p eq 'Web')` for exact platform match. For 'products' array, use `products/any(p: contains(tolower(p), 'teams'))` to search within product names. For 'releaseRings' array, use `releaseRings/any(r: contains(tolower(r), 'general'))` to filter by release ring. For 'cloudInstances' array, use `cloudInstances/any(c: contains(tolower(c), 'gcc'))` to filter by cloud instance. For 'availabilities' array, use nested object filtering: `availabilities/any(a: a/ring eq 'General Availability')` to filter by release ring, `availabilities/any(a: a/year eq 2026)` to filter by year, or `availabilities/any(a: a/month eq 'March')` to filter by month. Date range filtering example: `created ge 2025-01-01T00:00:00Z and created le 2025-12-31T23:59:59Z`. For complex date ranges spanning multiple months and years, ALWAYS use proper parentheses grouping: `(availabilities/any(a: a/year eq 2025 and (a/month eq 'November' or a/month eq 'December')) or availabilities/any(a: a/year eq 2026 and a/month eq 'January'))`. Complex example: `contains(tolower(title), 'teams') and platforms/any(p: p eq 'Desktop') and (availabilities/any(a: a/year eq 2025 and (a/month eq 'November' or a/month eq 'December')) or availabilities/any(a: a/year eq 2026 and a/month eq 'January'))`. To find roadmap items that are 'major changes', use contains(tolower(title), 'major') or contains(tolower(title), 'breaking') or contains(tolower(description), 'major') or contains(tolower(description), 'significant'). Do not use '$filter=isMajorChange+eq+true' as it is not supported by the API.",
        "schema": {
          "type": "string"
        },
        "explode": false
      },
      "Parameters.OrderByParameter": {
        "name": "$orderby",
        "in": "query",
        "required": false,
        "description": "Order results by specified field. Use 'created' for date ordering (not 'createdDateTime').",
        "schema": {
          "type": "string"
        },
        "explode": false
      },
      "Parameters.SkipParameter": {
        "name": "$skip",
        "in": "query",
        "required": false,
        "description": "Number of items to skip for pagination",
        "schema": {
          "type": "integer",
          "format": "int32",
          "minimum": 0,
          "default": 0
        },
        "explode": false
      },
      "Parameters.TopParameter": {
        "name": "$top",
        "in": "query",
        "required": false,
        "description": "Number of items to return (default: 10, max: 100)",
        "schema": {
          "type": "integer",
          "format": "int32",
          "minimum": 1,
          "maximum": 100,
          "default": 10
        },
        "explode": false
      }
    },
    "schemas": {
      "Models.Availability": {
        "type": "object",
        "required": [
          "ring",
          "year",
          "month"
        ],
        "properties": {
          "ring": {
            "type": "string",
            "description": "Release ring for this availability"
          },
          "year": {
            "type": "integer",
            "format": "int32",
            "minimum": 2020,
            "maximum": 2030,
            "description": "Year of availability"
          },
          "month": {
            "type": "string",
            "description": "Month of availability"
          }
        },
        "description": "Availability information for a release ring"
      },
      "Models.Error": {
        "type": "object",
        "required": [
          "code",
          "message"
        ],
        "properties": {
          "code": {
            "type": "string",
            "description": "Error code"
          },
          "message": {
            "type": "string",
            "description": "Error message"
          },
          "details": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Models.ErrorDetail"
            },
            "description": "Additional error details"
          }
        },
        "description": "Error information"
      },
      "Models.ErrorDetail": {
        "type": "object",
        "required": [
          "code",
          "message"
        ],
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        },
        "description": "Error detail information"
      },
      "Models.ErrorResponse": {
        "type": "object",
        "required": [
          "error"
        ],
        "properties": {
          "error": {
            "$ref": "#/components/schemas/Models.Error"
          }
        },
        "description": "Error response structure"
      },
      "Models.RoadmapItem": {
        "type": "object",
        "required": [
          "id",
          "title",
          "created",
          "description",
          "status"
        ],
        "properties": {
          "id": {
            "type": "integer",
            "format": "int32",
            "description": "Unique identifier for the roadmap item"
          },
          "title": {
            "type": "string",
            "description": "Title of the roadmap item"
          },
          "created": {
            "type": "string",
            "format": "date-time",
            "description": "Creation date and time of the roadmap item (ISO 8601 format). \nUse this field for date filtering, not 'createdDateTime'."
          },
          "description": {
            "type": "string",
            "description": "Detailed description of the roadmap item"
          },
          "status": {
            "type": "string",
            "enum": [
              "In development",
              "Rolling out",
              "Launched"
            ],
            "description": "Current development status of the roadmap item"
          },
          "modified": {
            "type": "string",
            "format": "date-time",
            "description": "Last modification date and time (ISO 8601 format)"
          },
          "generalAvailabilityDate": {
            "type": "string",
            "nullable": true,
            "pattern": "^\\d{4}-\\d{2}$",
            "description": "General availability date in YYYY-MM format"
          },
          "previewAvailabilityDate": {
            "type": "string",
            "format": "date-time",
            "nullable": true,
            "description": "Preview availability date (ISO 8601 format)"
          },
          "locale": {
            "type": "string",
            "nullable": true,
            "description": "Locale information"
          },
          "platforms": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Supported platforms for this feature"
          },
          "products": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Microsoft 365 products associated with this roadmap item"
          },
          "releaseRings": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Release rings for this roadmap item"
          },
          "cloudInstances": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Cloud deployment instances where this feature is available"
          },
          "moreInfoUrls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "description": "Additional information URLs related to this roadmap item"
          },
          "availabilities": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Models.Availability"
            },
            "description": "Release availability information with timing details for different release rings"
          }
        },
        "description": "A Microsoft 365 roadmap item with details about new features and updates"
      },
      "Models.RoadmapResponse": {
        "type": "object",
        "required": [
          "value"
        ],
        "properties": {
          "value": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Models.RoadmapItem"
            },
            "description": "Array of roadmap items"
          },
          "@odata.count": {
            "type": "integer",
            "format": "int32",
            "description": "Total count of items (when $count=true is specified)"
          },
          "@odata.nextLink": {
            "type": "string",
            "nullable": true,
            "description": "URL for next page of results (for pagination)"
          },
          "@odata.context": {
            "type": "string",
            "description": "OData context URL"
          }
        },
        "description": "Response containing roadmap items with OData metadata"
      }
    }
  },
  "servers": [
    {
      "url": "https://www.microsoft.com/releasecommunications/api/v2",
      "description": "Microsoft 365 Roadmap API",
      "variables": {}
    }
  ]
}
